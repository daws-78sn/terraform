**/.terraform/*

*.tfstate files
*.tfstate
*.tfstate.*

# Crash log files
crash.log
crash.*.log

# exclude all .tfvars files, which are likely to contain sensitive data such as 
# passwords and API/private keys, and other secrets. These should not be part of version control as they are data points
#which are potentially sensitive and subject to change depending on the environment

*.tfvars
*.tfvars.json

#ignore override files as they are usually used to override resources locally and so are not checked in
override.tf
override.tf.json
*.override.tf
*.override.tf.json

#include tfplan files to ignore the plan output of command:terraform plan -out=tfplan
#example: *tfplan

# ignore CLI configuration files
.terraformrc
terraform.rc